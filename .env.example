# Copy this file to .env and fill in real values. .env is gitignored - never commit real secrets.

POSTGRES_PASSWORD=
PGADMIN_PASSWORD=
JWT_KEY=
RESEND_API_KEY=
CLOUDFLARE_TUNNEL_TOKEN=
# Public URL the verification email link points to - your cloudflared
# domain in production, defaults to http://localhost:4200 if unset.
FRONTEND_BASE_URL=
